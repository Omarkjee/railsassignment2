<h1>Movies</h1>

<table>
  <thead>
    <tr>
      <%# Sortable headers %>
      <th class="sortable <%= 'asc' if sort_column == 'title' && sort_direction == 'asc' %> <%= 'desc' if sort_column == 'title' && sort_direction == 'desc' %>">
        <%= link_to "Title", movies_path(sort: "title", direction: sort_direction == "asc" && sort_column == "title" ? "desc" : "asc") %>
      </th>
      <th class="sortable <%= 'asc' if sort_column == 'rating' && sort_direction == 'asc' %> <%= 'desc' if sort_column == 'rating' && sort_direction == 'desc' %>">
        <%= link_to "Rating", movies_path(sort: "rating", direction: sort_direction == "asc" && sort_column == "rating" ? "desc" : "asc") %>
      </th>
      <th class="sortable <%= 'asc' if sort_column == 'release_date' && sort_direction == 'asc' %> <%= 'desc' if sort_column == 'release_date' && sort_direction == 'desc' %>">
        <%= link_to "Release Date", movies_path(sort: "release_date", direction: sort_direction == "asc" && sort_column == "release_date" ? "desc" : "asc") %>
      </th>
      <th>Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @movies.each do |movie| %>
      <tr>
        <td><%= movie.title %></td>
        <td><%= movie.rating %></td>
        <td><%= movie.release_date.strftime("%d-%b-%Y") if movie.release_date %></td>
        <td>
          <%= link_to 'Show', movie_path(movie) %> |
          <%= link_to 'Edit', edit_movie_path(movie) %> |
          <%= link_to 'Destroy', movie_path(movie), method: :delete, data: { confirm: 'Are you sure?' } %>
        </td>
      </tr>
    <% end %>
  </tbody>

  <tfoot>
    <tr>
      <td colspan="4">Total movies: <%= @movies.count %></td>
    </tr>
  </tfoot>
</table>

<%= link_to 'New Movie', new_movie_path, class: "button" %>
